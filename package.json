{
  "devDependencies": {
    "nodemon": "^3.1.0",
    "npm-run-all": "^4.1.5"
  },
  "scripts": {
    "start": "npm-run-all --parallel client:build server:start",
    "client:build": "cd client && npm run build",
    "server:start": "cd server && nodemon start"
  }
}
